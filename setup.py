from setuptools import setup, find_packages

setup(
    name='gpt-engineer',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List the packages that gpt-engineer depends on
        # For example: 'openai', 'flask', etc.
    ],
)
